package Utilities;

public enum ID{
    // This keeps track of all the IDs of the objects in the game
    Player(),
    Block(),
    Crate(),
    Trail(),
    Bullet(),
    BasicEnemy(),
    FastEnemy(),
    TrackerEnemy(),;
}
